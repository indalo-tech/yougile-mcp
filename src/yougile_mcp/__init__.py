"""YouGile MCP server."""

# The only place the version is written; pyproject.toml reads it, the release workflow checks
# that the git tag matches it, and CHANGELOG.md must have a section for it.
__version__ = "0.13.0"
